[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    auth: tests for authentication endpoints
    profiles: tests for profile endpoints
    matches: tests for matches endpoints
    messages: tests for messaging endpoints 